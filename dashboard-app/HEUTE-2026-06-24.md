# Tagesstand – 24.06.2026

## Heute umgesetzt

### 1. Neues Dashboard als eigene App-Struktur weiter aufgebaut

- `dashboard-app/` als neue, sauber getrennte Arbeitsbasis verwendet
- Aufteilung in `index.html`, `scripts/` und `styles/` weitergeführt
- Bestehendes altes Dashboard bleibt als Referenz erhalten und wird nicht weiter verbogen

### 2. Startseite, Plattformbereiche und Arbeitsfluss verbessert

- Startseite mit eigener Übersicht bleibt erhalten
- Bereiche für `Kulturverein` und `Mario Marschall` als getrennte Arbeitsflächen ausgebaut
- Inspector auf der Startseite ausgeblendet
- Inspector nur dort sichtbar, wo er fachlich Sinn ergibt

### 3. Editor von Scroll-Formular auf Schritt-für-Schritt umgebaut

- Neuer Eintrag / Bearbeiten läuft wieder als geführter Mehrschritt-Editor
- Oben statt großer Step-Navigation jetzt kompakter Fortschrittsbereich
- Schritte klar getrennt nach:
  - Inhalt
  - Termin / Ort / Zeiten
  - Veröffentlichung / Bild / Zusatzoptionen
- Live-Vorschau bleibt während der Bearbeitung sichtbar

### 4. Schnellauswahl und Vorlagen ergänzt

- Schnellauswahl für typische Uhrzeiten im Kulturverein eingebaut
- Schnellauswahl für häufig genutzte Orte ergänzt
- Schnellauswahl für Preisangaben ergänzt
- Schnellauswahl für Telefonnummer ergänzt
- Mario-Button-Vorlagen ergänzt:
  - `Ticket reservieren`
  - `Mehr erfahren`
  - `Individuell`

### 5. Mario- und Vereinslogik weiter vereinheitlicht

- Cross-Publishing zwischen Mario und Kulturverein weiter ausgebaut
- Verknüpfte Einträge können gemeinsam behandelt werden
- Verknüpfungen können gelöst werden
- Sprunglogik zwischen verknüpften Einträgen vorbereitet bzw. eingebunden

### 6. Ablauf für abgelaufene Vereins-Veranstaltungen eingebaut

- Erkennung für abgelaufene Kulturverein-Events ergänzt
- Wenn ein Event abgelaufen ist und das Ticketformular noch als offen gilt, erscheint ein Hinweis
- Zuerst muss bestätigt werden, dass das Ticketformular entfernt wurde
- Danach kann entschieden werden, ob der Eintrag:
  - archiviert wird
  - oder gelöscht wird
- Archivierte bzw. abgelaufene Einträge werden im normalen Arbeitsfluss sauberer behandelt

### 7. Statusdarstellung in den Karten verbessert

- Bei `Ticketformular erledigt` keine auffällige Text-Pille mehr in den Karten
- Offene Fälle kompakter und deutlicher markiert
- Statusfarben für `aktiv`, `bald`, `abgelaufen`, `archiviert` weiter vereinheitlicht

### 8. Vorschau- und Inspector-Verhalten überarbeitet

- Inspector auf Home nicht mehr dauerhaft angezeigt
- Vorschauen stärker an die echten Ziel-Layouts angenähert
- Mario- und Kulturverein-Vorschau bleiben getrennt gedacht
- Arbeitsbereich soll eher wie die echte Ausgabe wirken und nicht wie eine generische Admin-Karte

### 9. Popup / Editor visuell überarbeitet

- Editor-Modal optisch beruhigt und strukturierter gemacht
- Linke Eingabefläche klarer vom rechten Vorschau-Bereich getrennt
- Fortschrittsanzeige hochwertiger gestaltet
- Schnellauswahl-Chips und Vorlagenkarten optisch verbessert
- Mobile-Abstände und Desktop-Hierarchie im Popup nachgezogen

### 10. Begleitdokumente ergänzt

- Strukturdatei für den Neuaufbau erstellt
- Arbeitsanweisungen für parallele Zusammenarbeit mit Claude angelegt

## Aktueller Stand

Das neue Dashboard ist jetzt deutlich näher an einem echten produktiven Arbeitswerkzeug:

- klarerer Ablauf
- bessere Trennung der Plattformen
- bessere Live-Vorschau
- Schritt-für-Schritt-Erfassung statt langer Formularseite
- wichtigere Sonderfälle für abgelaufene Events bereits berücksichtigt

## Offene bzw. spätere Punkte

- Toast/Bestätigung noch einmal in Ruhe prüfen
- Popup-Design bei Bedarf weiter verfeinern
- Mobile-Nutzung als Web-App / PWA später gesondert aufbauen
- Login / Zugriffsschutz noch nicht umgesetzt
- Feinschliff für Live-Vorschau kann noch weiter verbessert werden

---

## Session-Ergänzungen – 24.06.2026 (Nachmittag)

### 11. Mario-Tab: neues 2-Spalten-Layout (Inspector oben)

- Mario-Tab bekommt eigenes Layout via `.workspace--mario` CSS-Klasse (per JS beim Tab-Wechsel gesetzt)
- Inspector wandert nach oben, volle Breite (`order: -1`)
- Innerhalb des Inspectors: Live-Vorschau links (`2fr`), Ops-Info rechts (`1fr`)
- Event-Kartenliste darunter im 2-Spalten-Grid
- Workspace-Spalten: linke Liste schmaler (`0.85fr`), Inspector-Bereich breiter (`1.1fr`)

### 12. Mario-Inspector: Aktionen hinter Zahnrad-Toggle

- Aktions-Buttons hinter Gear-Button versteckt (`.inspector-actions-toggle`)
- Beim Öffnen erscheint eine volle Breite-Leiste mit eckigen Buttons in einer Zeile
- Buttons sortiert: Neu → Bearbeiten → Löschen → Pausieren | (Gap) Zu KV übernehmen → Verknüpfung lösen
- „Zum Kulturverein springen" ausgeblendet (per `display: none`)
- Erste 4 Buttons schmaler (`flex: 0.7`), KV-Buttons nehmen restlichen Platz (`flex: 1`)
- JS-Handler `toggle-inspector-actions` ergänzt in `handleActionClick`

### 13. Umbenennungen und Textkorrekturen

- „Deaktivieren" / „Aktivieren" → „Pausieren" / „Fortsetzen"
- „Neuer Eintrag" (im Inspector) → „Neu"
- „Löschen"-Button: Umlaut-Fehler `Loeschen` → `Löschen` behoben

### 14. Mario-Eventliste: „Neuer Termin"-Button + eckige Zählpille

- In der Mario-Panel-Kopfzeile: Pille (`#mario-count`) + neuer „+ Neuer Termin"-Button nebeneinander im `.surface-head-aside`-Container
- Pille `border-radius` auf `6px` reduziert (eckig statt rund)
- Button kompakt (`surface-new-btn`), nutzt bestehenden `create-event`-Handler

### 15. Live-Vorschau ans echte Widget angeglichen

- Bild-Spalte: `padding: 16px`, heller Hintergrund, Bild mit `border-radius: 14px` und Shadow — wie auf der echten Mario-Seite
- Bild: `height: auto` statt `object-fit: cover` → echtes Seitenverhältnis, kein Crop
- `border-left`: von `3px / 25%` auf `1px / 12%` reduziert — dezenter, passend zum Widget
- Datum und Titel: `Georgia, serif` statt Dashboard-Font — entspricht dem Widget

### 16. Mobile-Anpassungen Mario-Layout

- **1100px**: Preview-Poster stapelt sich (flex-column), Inspector bleibt oben, Event-Liste 1 Spalte
- **820px**: Aktionsleiste scrollt horizontal wenn zu eng
- **640px**: Mario-Card einspaltig (Bild oben), Aktionsleiste als 3×2-Raster (je 2 Reihen à 3 Buttons), Touch-Targets min. 44px, `surface-head-aside` stapelt sich

---

## Session – 29.06.2026

### 17. KV Inspector: Aktionen hinter Zahnrad-Toggle (wie Mario)

- Aktionsbuttons im KV-Inspector hinter Gear-Button (⚙) versteckt — gleiches Muster wie bei Mario
- Eigene CSS-Klasse `.kv-inspector-actions-toggle` für helles KV-Design (statt dunklem Mario-Stil)
- Geöffneter Zustand: rosé/lila Akzent passend zur KV-Farbe
- Vorschau-Karte selbst wurde **nicht** verändert — nur die Aktionsleiste

### 18. KV Inspector: Aktionsraster 3 Spalten × 2 Zeilen

- KV hat weniger Platz als Mario → Buttons als `grid` mit `repeat(3, 1fr)`
- Innentrennlinien per Border-Trick gelöst (kein `gap`, damit keine Lücken im Raster)
- Letzter Button je Zeile ohne rechte Border, letzte Zeile ohne untere Border

### 19. KV Editor: Bild als erster Schritt

- Neuer Schritt 0 (nur KV): Bild vom Gerät hochladen
- Datei-Input mit Canvas-Resize → JPEG base64 (max 800px, Qualität 0.72)
- Vorschau wird sofort im Editor aktualisiert
- Kein Firebase Storage nötig — Bild direkt als `data:image/...` im Firestore-Dokument

### 20. KV Editor: Alle Schritte als Feldblock-Container neugebaut

- Jedes Eingabefeld ist jetzt ein eigener `.editor-field-block` mit Hintergrund + Border
- Schnellauswahl-Chips stehen oben im Block, Eingabefeld darunter
- Reihenfolge im Termin-Schritt: Datum → Einlass+Beginn → Ort → Preis → Telefon
- Inhalt-Schritt: Titel / Untertitel (KV) / Beschreibung
- Ausgabe-Schritt: Button-Vorlage / Button-Label / Link (Mario) + Bild-URL (beide) + Toggles
- Umbau gilt für **beide Typen** (KV und Mario nutzen dieselbe Panel-Struktur)

### 21. Chips: eckig und zentriert

- `border-radius` von `999px` (Pill) auf `7px` reduziert — kantig mit leichter Rundung
- Chips innerhalb von Feldblöcken: `justify-content: center`
- Ort-Chip: vorher doppelt (einmal KV-Stil, einmal Mario-Stil) → jetzt ein einziger Chip für beide

### 22. Mobile Editor: Button-Logik bereinigt

- `Abbrechen` auf Mobile ausgeblendet (nur Weiter + Zurück bleiben)
- `Speichern` und `Zurück` haben jetzt `hidden` als HTML-Startzustand → kein Aufblitzen beim Öffnen
- `Speichern` ersetzt `Weiter` nur auf dem letzten Schritt — nie gleichzeitig sichtbar
- Einlass/Beginn stapeln sich auf Mobile untereinander (`grid-template-columns: 1fr`)

### 23. Bugfix: Bildvalidierung für Galerie-Uploads

- Validierung hat `data:image/...`-URLs (vom Gerät hochgeladen) fälschlicherweise abgelehnt
- Regex erweitert: `^(https?:\/\/|data:image\/)` — beide Formate werden jetzt akzeptiert

---

## Session – 19.07.2026

### 24. GitHub Pages live geschaltet

- Repo `KV-Dashboard` (origin) auf GitHub Pages aktiviert: Branch `main`, Ordner `/ (root)`
- Live-Link zum Testen (u.a. auf dem Handy):
  `https://yorkd87-a11y.github.io/KV-Dashboard/dashboard-app/`
- Lokaler Dev-Server für Vorschau ergänzt: `.claude/launch.json` + `.claude/static-server.js` (Node, ohne externe Abhängigkeiten)

### 25. Startseite: Header und Schnell-hinzufügen-Block umgebaut

- Icon oben links im Header eingefügt (`assets/icons/dashboard-icon.png`, vorher `Dashboard-Icon-bg-removed.png` im Projektordner) — ersetzt den bisherigen Text-Badge „MM"
- Header-Eyebrow „Neues Dashboard" → „Dashboard"
- Intro-Bereich auf der Startseite (vorher „Arbeitsfläche" mit Überschrift + Fließtext) reduziert auf eine schlichte Überschrift „Schnell hinzufügen" + die Buttons — Überschrift-Text und Beschreibungstext entfernt
- Buttons eckiger gemacht (`border-radius: 8px` statt `999px`, passend zum Rest der App) und Schrift etwas kleiner (`0.85rem`), Klasse `.quick-add-button` scoped auf `.intro-actions`, damit andere Primary-/Secondary-Buttons in der App (Editor, Confirm-Dialog etc.) unverändert bleiben
- Button-Beschriftungen gekürzt: „Neues Vereins-Event" → „Vereins-Event", „Neuen Mario-Termin" → „Mario Event"
- Dritter Button „Weitere Seite" als Platzhalter ergänzt (disabled, Tooltip „Bald verfügbar") — siehe TODO unten

### 26. Header-Alert für offene Prüfungen (Ticketformular/Ablauf)

- Neuer roter „!"-Button im Header (`#headerAlertButton`), rechts neben Jotform-Link / links vom Burger-Menü auf Mobile
- Sichtbar, sobald mindestens ein KV-Event geprüft werden muss: offenes Ticketformular (`getPendingKvEvents`) oder abgelaufen mit unbestätigter Ticketformular-Entfernung (`getExpiredKvEventsNeedingCleanup`)
- Klick: springt zum Kulturverein-Tab, wählt den betroffenen Eintrag aus und scrollt zur Inspector-Karte (`.preview-workflow`), die den Status/die nötige Aktion zeigt — dieselbe Karte wie beim manuellen Auswählen des Eintrags
- Aktualisiert sich automatisch bei jedem `render()`, also auch nach Firestore-Live-Updates

## Offene Punkte / TODO

- **Dritte Plattform/Seite (Platzhalter-Button „Weitere Seite" auf der Startseite):**
  - Braucht eigene Firebase-/Firestore-Anbindung im selben Stil wie KV und Mario (eigene Collection oder eigenes Feld zur Unterscheidung, analog zu `events-service`-Logik)
  - Geplanter Inhalt des Blocks: **Bild + Textblock + „Anrufen"-Button**
  - Bild und Text sollen per Upload austauschbar sein (ähnlich wie bestehender Bild-Upload-Schritt im Editor)
  - Name/Bezeichnung der dritten Seite noch offen — mit User klären
  - Bis dahin bleibt der Button bewusst `disabled` in `index.html`
- Icon-Datei ist mit ca. 2,2 MB recht groß für ein 48px-Logo (1254×1254 Original) — bei Gelegenheit verkleinern/komprimieren (kein Bildtool wie ImageMagick auf diesem Rechner installiert, daher noch nicht automatisch erledigt)

---

*Schluss für heute.*
